// The renderer entrypoint intentionally stays small. Feature orchestration is
// in app/AppShell.jsx; session side effects live in hooks/useCosightSession.js.
export { default } from './app/AppShell.jsx'
