# Cosight 开发原则

## 产品定位

- 核心方向是：通用对话、屏幕视觉理解，以及屏幕绘画能力。

## 架构原则

- Cosight 是 Windows 上的多模态实时运行时：AI 能听见用户、看见用户分享的屏幕，并通过语音回应。
- 采用 `Core + Plugin` 架构。Core 提供对话、屏幕捕获、实时模型连接、音频输入输出和会话状态；Plugin 在 Core 之上增加领域工作流。
- 插件不得反过来定义 Core 的产品身份，也不得把插件专属文案、流程或数据模型硬编码进核心聊天页面。
- 后续需要分析或评审时，优先由外部文本 Agent 消费结构化的 `Session Artifact`，不要把分析逻辑耦合到实时会话核心。

## MVP 优先级

- MVP 优先保证“实时语音对话 + 屏幕捕获 + 屏幕视觉理解 + 动态追问”的闭环稳定可用。
- Qwen Omni Realtime 是当前 MVP 的首选模型路线；通过 Python bridge 对接，保持模型 URL、API Key 和 model name 可配置，以便替换兼容的实时模型。
- 屏幕绘画、叠加层和更复杂的视觉交互属于核心产品的演进方向，应在稳定的对话和屏幕运行时之上逐步加入。
- 透明画布是 Core 的可选能力，Default 角色默认启用；其他角色由 Role 授权后，Agent 才能通过受限的绘画工具在实际屏幕预览上绘制，用户不直接使用画笔。
- Agent 绘画工具使用相对于屏幕捕获画面的归一化坐标，并必须返回绘制成功或失败结果；未启用透明画布时，模型初始化提示必须明确说明不可绘画。
- 绘画支持内部的自适应局部定位流程：目标较小、靠近边缘或整屏定位不确定时，Agent 先调用 `focus_screen_region` 提供粗略中心，客户端从真实捕获帧裁切约半幅并放大约 2 倍，边缘位置自动贴合捕获帧；Agent 再用 `coordinateSpace=focused_region` 调用绘画工具，客户端将局部归一化坐标映射回整屏。该流程属于 Drawing 内部策略，不新增独立能力授权，也不对每一帧持续裁切。
- 透明画布本身不直接参与桌面回采，避免画面递归；发送给模型的后续屏幕帧必须合成当前 Agent 标注，使 Agent 能看见并复核自己的绘制结果。
- 绘画覆盖层只支持整屏捕获。窗口捕获的视频使用窗口局部坐标，而透明覆盖层使用显示器坐标；没有可靠窗口 bounds 时，禁止把窗口来源映射到整屏覆盖层，客户端必须拒绝绘画并提示用户改为分享整屏。
- 绘画工具每次都必须明确声明 `coordinateSpace`；`focus_screen_region` 成功后只能使用 `focused_region`，不得静默回退到 `full_screen`。
- 实时会话处于 Connecting 阶段时锁定共享按钮，避免能力更新早于 session ready；Connected 后允许停止共享并重新选择来源，整屏/窗口变化必须同步更新 Drawing（包含屏幕写字）工具，停止共享时还要停止 Core 字幕输出。
- 用户要求重新检查或修正任意 Agent 绘制时，必须依据包含上一轮绘制的最新屏幕帧复核；替换旧绘制时先清除再重绘，追加内容时才保留旧绘制。
- Agent 框选屏幕对象时，绘画 Prompt 必须遵循“定位目标 → 记录完整捕获帧边界并换算坐标 → 绘制闭合边界 → 读取下一帧复核与修正”的流程，不能只凭目标中心或 Cosight 预览尺寸估算。
- 角色中的 `Drawing`（绘画）是统一的视觉输出能力，授权后由智能体通过绘画工具和内部的 `show_caption` 工具在实际共享屏幕上绘制图形、文字、标签或提示；`show_caption` 的兼容实现仍可维护在 `abilities/writing/`，但不得作为独立 Role 能力暴露。
- Core `Subtitles`（字幕）不是 Role 能力，而是 Settings 下的核心开关。开启后，客户端根据每次 Cosight 语音回复产生的 realtime audio transcript 自动显示字幕；它与 Drawing 使用同一透明覆盖层并在发送给模型的屏幕帧中合成。
- 可扩展能力统一放在根目录 `abilities/` 下；绘画及其屏幕写字子能力共同归入 Drawing 的产品契约。读屏幕、听、说属于基础实时链路，继续由 Core bridge 管理，不单独拆能力目录。
- 未来新增的可扩展能力必须放在根目录 `abilities/` 文件夹下，不得在核心聊天页面或基础 bridge 中随意散落实现。每项能力应建立独立子文件夹，并在其中维护相关 prompt、脚本、工具契约和必要的说明文档。
- Drawing 内部的文字层、Core 字幕和图形层可以同时启用，清除其中一种内容时不得清除另一种内容；发送给模型的后续屏幕帧应同步合成三者。
- 任何新功能都应先确认属于 Core 还是 Plugin，再决定代码放置位置、界面文案和会话数据归属。

## 技术边界

- Windows 客户端使用 Electron。
- 当前先使用 Python 对接 Qwen Omni Realtime。
- 客户端负责麦克风、屏幕捕获、设备选择、音频播放和会话 UI。
- Python bridge 负责实时模型连接；模型通过 WebSocket 通信。
- Windows 发布必须把 Python runtime、`dashscope`、知识文件解析依赖和两个 Python 入口一起打包进安装包；最终用户不应被要求单独安装 Python、Node.js、pip 或其他运行时依赖。`scripts/package-windows.ps1` 是标准的一键打包入口，生产环境 Electron 必须启动打包后的 bridge executable，而不是依赖系统 `python`。
- 打包资源必须包含 `data/sample-roles.json` 以及能力目录中的模型 prompt；用户配置、API Key、日志和知识文件继续保存在 `%APPDATA%\\cosight`，不得作为安装包资源或 Git 文件提交。
- 设置中允许用户添加多个自定义模型，每个模型包含 URL、API Key 和 model name，并允许用户选择当前模型。
- 模型配置属于 Core，但必须通过左侧 `Models/模型` 独立页面管理；该页面负责添加、编辑、删除和选择当前模型。Settings/设置页面只管理设备、连接和行为开关，不放置模型管理表单。
- Settings/设置必须是左侧导航进入的独立主页面，不使用右侧抽屉或侧边设置面板；页面内集中管理设备、连接、界面语言和对话行为开关。
- Settings 提供 Core `Subtitles` 开关；当前实现默认开启，并通过客户端本地设置持久化。它只控制语音回复字幕的显示，不改变 Role 能力授权。

## 交互原则

- 未分享屏幕时，屏幕区域显示黑屏，不显示虚假的架构图或占位视觉内容。
- 分享屏幕后，屏幕区域显示实际捕获的屏幕或窗口画面。
- transcript 只显示真实会话内容，不预置虚假聊天记录。
- 麦克风区域显示真实音量变化；底部麦克风图标必须是可用的 mute/unmute 切换按钮，点击后实际启用或停用麦克风音轨。
- 界面中的窗口控制或其他按钮必须真实可用，不添加虚假的最小化、最大化、关闭或 Expand 控件。
- 屏幕源选择器必须能在不同屏幕和窗口数量下正常滚动并选择任意源。

## Role 角色系统

- Role 是 Core 的可复用 prompt profile。
- Chat Session 顶部提供角色选择；默认角色为 `Default`。聊天进行中角色选择必须禁用，角色只在下一次聊天启动时生效。
- Roles 页面负责新增、编辑和删除角色；Chat Session 的选择菜单可以通过加号跳转到同一个新增角色页面。
- 角色包含姓名、身份、目的、行动方式、对话流程、约束、语种、音色、能力和知识。
- 角色可以配置可选头像；头像属于角色身份展示信息，应在角色列表和会话角色选择中保持一致。
- 语种与音色是单选项；能力是多选项，角色勾选的能力就是本次会话的实际生效能力。Abilities 页面只展示能力目录，不提供独立启用开关；所有能力授权统一在 Role 中完成。
- Role 勾选 Drawing 时才显示并保存绘画策略；该策略同时指导图形绘制和屏幕写字。旧角色中的 `writingPolicy` 仅作为兼容数据保留，并在 Drawing 启用时一并注入；未勾选 Drawing 时相关策略必须清空、隐藏且不得注入会话 prompt。Core Subtitles 不读取角色策略。
- 语种通过会话 prompt 约束：选定中文或英文时使用硬性语言锁定，模型不得因为用户使用其他语言而切换输出语言；选择自动时不施加固定语言锁定，跟随用户语言自然回复。代码、URL、专有名词和必要引用可以保留原文。音色在 Omni Realtime 会话参数中设置。角色切换不得在活动会话中修改当前实时 session。
- Knowledge 同时支持文本和文件。文件复制到应用数据目录后再使用，启动会话时以参考资料形式加入角色 prompt；知识内容中的指令不能覆盖系统规则、角色配置或用户请求。
- Initiative（主动性）是可选能力。Core 在 `server_vad` 下本地累计检测用户和智能体双方的静默，并通过 `response.create` 客户端事件主动触发；不得用 `idle_timeout_ms` 替代这一机制。沉默秒数和触发后的对话规则属于 Role 属性；任意一方说话都必须把累计静默时间重置为 0。
- Initiative 只有在同一个 Role 同时勾选 Listening 和 Speaking 时才可启用；角色编辑页必须执行这个依赖约束。检测用户麦克风音量和智能体音频输出都属于“说话”活动来源。
- 只有当 Role 勾选 Initiative 时，角色编辑页才显示沉默秒数和主动触发规则；秒数必须是整数，默认 10 秒，范围限制为 5～300 秒。未勾选时不提供这些字段，也不向会话启用主动触发。
- 官方示例角色维护在项目资源 `data/sample-roles.json` 中，未来打包时必须随应用资源进入安装包；它们用于新用户开箱即用地了解 Cosight 的角色能力，不是用户本地配置的替代品。
- 官方示例角色在运行时以只读方式展示，只能选择使用，不能直接编辑或删除。用户本地角色优先于同 ID 的内置示例，以避免升级或开发环境出现重复角色，也不得覆盖用户数据。
- 官方示例角色资源不得包含 API Key、用户知识文件的绝对路径或其他本机敏感数据。需要同步当前角色时使用 `scripts/sync-sample-roles.mjs`，文件知识必须另行作为明确的应用资源管理。
